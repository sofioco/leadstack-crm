# ======================
# Firebase Client SDK
# ======================
NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
NEXT_PUBLIC_FIREBASE_APP_ID=

# ======================
# Firebase Admin SDK
# ======================
FIREBASE_ADMIN_PROJECT_ID=
FIREBASE_ADMIN_CLIENT_EMAIL=
# Paste the private key from your service account JSON (include the -----BEGIN/END----- markers)
FIREBASE_ADMIN_PRIVATE_KEY=

# ======================
# Stripe
# ======================
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
STRIPE_SECRET_KEY=

STRIPE_WEBHOOK_SECRET=
STRIPE_FOUNDERS_PRICE_ID=

# Canonical self-serve Solo subscription price. The app also accepts the
# legacy STRIPE_STARTER_PRICE_ID name as a fallback for older deployments.
STRIPE_SOLO_PRICE_ID=

# Base plan prices — self-serve checkout on the landing page pricing
# section. "Scale"/"Broker" and "Luxury" are deliberately sales-assisted
# ("Talk to us") and don't need a price id here — see pricing.tsx.
STRIPE_STARTER_PRICE_ID=
STRIPE_PRO_PRICE_ID=
STRIPE_SCALE_PRICE_ID=

# Add-on prices — only these 3 landing-page add-ons have a real in-app
# feature gate to auto-activate on purchase (IDX Listings, Social Planner,
# AI Website Studio). Leave any of these unset and that add-on's checkout
# button/section returns a friendly "not configured yet" error instead of
# breaking. See src/lib/stripe/catalog.ts.
STRIPE_ADDON_IDX_PRICE_ID=
STRIPE_ADDON_SOCIAL_PRICE_ID=
STRIPE_ADDON_WEBSITE_STUDIO_PRICE_ID=

# Optional — a Stripe Coupon id applied automatically when a self-serve
# signup selects 3+ add-ons at once (the landing page's "stack 3+, save
# 15%" banner). Leave unset and the discount just doesn't apply yet.
STRIPE_ADDON_BUNDLE_COUPON_ID=

# ======================
# App
# ======================
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Server-only deployment entitlement. Default false preserves hosted billing.
# Set true only for a self-hosted installation; applies to every agency here.
# Authentication, email verification, roles and tenant access still apply.
SELF_HOSTED_MODE=false

# Optional: mounts the real AgentStack web-chat widget on public marketing
# pages (home, signup, about, legal, future compare pages) instead of the
# fallback Crisp bubble. Point this at a dedicated sales/demo sub-account
# whose AI persona is configured to answer pricing, feature, and FAQ
# questions for your public site.
NEXT_PUBLIC_MARKETING_WEB_CHAT_SUBACCOUNT_ID=

# Optional: YouTube video id used by the hero presentation modal. The
# current landing layout stays the same — when this is set, the existing
# presentation block opens the walkthrough in a modal.
NEXT_PUBLIC_MARKETING_HERO_VIDEO_ID=

# Bootstrap admin email — only this email is allowed to claim the admin slot
# on first signup. Set this BEFORE deploying so a stranger can't grab admin
# during the deploy window. After admin is claimed, this var is ignored..
BOOTSTRAP_ADMIN_EMAIL=

# ======================
# Cookie Secrets (for next-firebase-auth-edge)
# Generate with: openssl rand -base64 32
# ======================
COOKIE_SECRET_CURRENT=
COOKIE_SECRET_PREVIOUS=

# MCP remote-client OAuth signing secret. Use a separate random value in
# production; COOKIE_SECRET_CURRENT is only a local fallback.
MCP_OAUTH_SECRET=

# ======================
# Email (Resend)
# Shared sender — get an API key at https://resend.com and verify your
# sending domain under Domains. EMAIL_FROM must be on a verified domain.
# ======================
RESEND_API_KEY=
EMAIL_FROM="AgentStack <notifications@example.com>"

# ======================
# SMS (Twilio)
# Shared sender — get credentials at https://console.twilio.com. Trial
# accounts can only send to verified numbers and prepend a trial banner.
# ======================
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=

# ======================
# Automations / Workflow Recipes (Upstash QStash)
# Get keys at https://console.upstash.com/qstash. The signing keys
# (current + next) verify the inbound POST signature on the step
# executor route; rotate by promoting NEXT to CURRENT.
# QSTASH_URL is the region endpoint shown in the QStash dashboard's
# Quickstart panel — required because the SDK's default doesn't always
# match the token's region.
# Without these, the automation step route returns 503 (graceful fail).
# ======================
QSTASH_URL=
QSTASH_TOKEN=
QSTASH_CURRENT_SIGNING_KEY=
QSTASH_NEXT_SIGNING_KEY=

# Server-side secret used to HMAC unsubscribe tokens. Generate with:
# openssl rand -base64 32
# Rotating this value invalidates every outstanding unsubscribe link.
AUTOMATIONS_TOKEN_SECRET=

# ======================
# Website builder (gitpage.site)
# Single agency-level API key, shared by every sub-account. Generate at
# https://www.gitpage.site Dashboard -> API Access -> Generate API Key.
# Format: gp_<64 hex chars>. Rotating regenerates and invalidates the
# previous key.
# GITPAGE_API_URL is optional; defaults to https://www.gitpage.site.
# ======================
GITPAGE_API_KEY=
GITPAGE_API_URL=

# ======================
# Vercel API (optional — powers automatic custom-domain registration)
# Without these, "Connect Domain" still saves the domain and checks DNS, but
# cannot confirm — or perform — the separate step of attaching the domain to
# this project on Vercel. DNS pointing here is NOT enough on its own: Vercel
# refuses traffic for a domain it hasn't been told to expect, so without this
# a customer can do everything right and still see Vercel's own "Domain not
# configured" page. See src/lib/domains/vercel-api.ts for the full rationale.
#
# VERCEL_TOKEN   — vercel.com/account/tokens -> Create Token. Scope it to the
#                  team that owns this project if you use a team account.
# VERCEL_PROJECT_ID — Project Settings -> General -> Project ID.
# VERCEL_TEAM_ID    — only needed if this project lives under a Vercel team,
#                  not a personal account. Same Project Settings page.
# ======================
VERCEL_TOKEN=
VERCEL_PROJECT_ID=
VERCEL_TEAM_ID=

# Set to "off" to disable the daily liveness heartbeat to gitpage. The
# heartbeat reports anonymous deployment metadata (instance id, owner
# email, version, sub-account count, builds-last-day, platform) and
# returns whether the agency owner has an active website-builder
# subscription so the UI can show the right CTA. Default: enabled.
# GITPAGE_TELEMETRY=off

# ======================
# AI SMS replies (optional)
# Powers the per-sub-account "AI Replies" feature in Settings. When a
# lead texts a sub-account's dedicated Twilio number, an LLM agent
# replies in real time using the configured persona + safety rails.
#
# Get a key at https://openrouter.ai. One key covers every supported
# model — Anthropic Claude, OpenAI GPT, Google Gemini, etc.
#
# Cost: default model (Claude Haiku 4.5) is ~$0.005-0.02 per SMS
# exchange. Per-sub-account override to Opus 4.7 etc. is ~50x more.
#
# Without these vars, the AI Replies settings panel renders but the
# toggle stays inert — incoming SMS only triggers the existing chat
# thread storage, no AI reply fires.
# ======================
OPENROUTER_API_KEY=

# Optional authentication for the r.jina.ai public-page reader used by
# Business Blueprint imports. Without it, portal reads share Jina's much lower
# anonymous rate limit. Create a Reader API key at https://jina.ai/reader/.
JINA_READER_API_KEY=

# Default OpenRouter model id when no per-sub-account override is set.
# Leave blank to use Claude Haiku 4.5 (recommended cost/quality balance).
# AI_REPLIES_DEFAULT_MODEL=anthropic/claude-haiku-4.5
#
# Optional emergency model used only after OpenRouter returns HTTP 402.
# Disabled by default because silent model downgrades change structured-output
# quality and may use providers with different data-handling terms.
# Set a reviewed model id only if that tradeoff is intentional.
# AI_REPLIES_CREDIT_FALLBACK_MODEL=off

# ======================
# Firecrawl (optional — powers the AI Agent Knowledge Base's url/crawl sources)
# When set, the AI Agents -> Overview -> Knowledge Base section can add
# "Webpage or PDF" and "Crawl a site" sources — Firecrawl fetches the
# content, which then gets chunked + embedded (see OPENAI_API_KEY below).
# Get a key at https://firecrawl.dev -> Dashboard -> API Keys.
# One key per agency deployment — shared across all sub-accounts.
# ======================
FIRECRAWL_API_KEY=

# ======================
# OpenAI embeddings (optional — powers the AI Agent Knowledge Base's retrieval)
# A separate provider from OPENROUTER_API_KEY (OpenRouter only proxies chat
# completions, not embeddings). Used to embed every Knowledge Base chunk at
# ingest time and the visitor's message at reply time, so only the most
# relevant chunks get pulled into the AI's prompt — see "Knowledge Base v2
# (RAG)" below for the full architecture.
# Get a key at https://platform.openai.com -> API keys.
# Cost: text-embedding-3-small is ~$0.02 per 1M tokens — negligible even
# at high message volume.
# Without this, the Knowledge Base section still renders and sources can
# still be added, but ingestion returns a friendly "OpenAI embeddings
# aren't configured" error and sources stay stuck at "pending".
# ======================
OPENAI_API_KEY=

# ======================
# Vapi (optional — powers the AI Voice Agent channel)
# Sign up at https://vapi.ai -> Dashboard -> API Keys for the private
# API key. The webhook secret is anything you generate locally and paste
# into the Vapi assistant's "Server URL Secret" field — Vapi sends it
# back as `Authorization: Bearer <secret>` on every webhook to us so we
# can verify the call came from Vapi and not a stranger who guessed our
# webhook URL. Generate with: openssl rand -base64 32
# Without these the AI Agents -> Voice settings page renders but the
# Enable toggle returns 503 with a friendly "Vapi not configured" error.
# ======================
VAPI_API_KEY=
VAPI_WEBHOOK_SECRET=

# ======================
# Meta — Facebook Messenger + Instagram DM inbox (optional, preview)
# Powers the agency-gated Facebook + Instagram inbox preview channels. Create a
# Meta app at https://developers.facebook.com (Business type), add Messenger +
# Instagram, and complete App Review for pages_messaging +
# instagram_manage_messages before it works for non-testers. Register the
# webhook callback (https://YOUR-DOMAIN/api/webhooks/meta) with the verify
# token below, and add https://YOUR-DOMAIN/api/sub-accounts/{id}/meta/callback
# as a valid OAuth redirect URI. Without these, the preview gate + settings card
# still render but the Connect button reports "not configured on this
# deployment". OAuth `state` is signed with AUTOMATIONS_TOKEN_SECRET (reused).
# ======================
META_APP_ID=
META_APP_SECRET=
META_WEBHOOK_VERIFY_TOKEN=

# ======================
# Google OAuth (optional)
# Blueprint import from Google Business Profile. Agents can authenticate with
# their Google account to import profile data directly instead of pasting a link.
# Set up at https://console.cloud.google.com/credentials:
#   1. Create a new OAuth 2.0 Client ID (Web application)
#   2. Add authorized redirect URIs:
#      https://YOUR-DOMAIN/api/sub-accounts/[id]/business-profile/import-google
#   3. Paste the Client ID and Secret below
# Without these, the Blueprint import UI still shows the web link import option,
# just not the Google Business Profile option.
# ======================
# Calendar OAuth (server-side authorization code flow)
GOOGLE_CALENDAR_CLIENT_ID=
GOOGLE_CALENDAR_CLIENT_SECRET=
MICROSOFT_CALENDAR_CLIENT_ID=
MICROSOFT_CALENDAR_CLIENT_SECRET=

GOOGLE_OAUTH_CLIENT_ID=
GOOGLE_OAUTH_CLIENT_SECRET=

# ======================
# Crisp Chat (optional)
# Live-chat widget loaded on every page. Get a Website ID at
# https://app.crisp.chat -> Settings -> Website Settings -> Setup
# Instructions. Leave blank to disable the widget entirely.
# ======================
NEXT_PUBLIC_CRISP_WEBSITE_ID=

# ======================
# Meta Pixel (optional)
# Site-wide Meta (Facebook) Pixel. When set, every page loads the pixel
# and fires PageView automatically; hosted form pages (/f/[id]) also
# fire a `Lead` event on successful submission. Find your Pixel ID at
# Meta Events Manager -> Data Sources. Leave blank to disable.
# ======================
NEXT_PUBLIC_META_PIXEL_ID=

# ======================
# Google Tag Manager (optional)
# Site-wide GTM container. Useful as the escape hatch for any tracking
# tag Meta Pixel doesn't already cover (LinkedIn, TikTok, Hotjar, custom
# server-side events via gtag, etc). Format: GTM-XXXXXXX.
# ======================
NEXT_PUBLIC_GTM_ID=

# ======================
# Leads map (Mapbox)
# Drives the "Where your leads are" map on the sub-account dashboard.
# Free tier: 50,000 map loads / month.
#   1. Sign up at https://account.mapbox.com
#   2. Account -> Tokens -> Create a token (default public scopes are fine)
#   3. Paste the pk.* token below
# Without this, the dashboard renders a "Mapbox not configured" card
# instead of the map — everything else still works.
# ======================
NEXT_PUBLIC_MAPBOX_TOKEN=

# ======================
# Landing-page urgency (AgentStack-variant only — optional)
# Read at build time; changing requires a rebuild/redeploy.
# Leave blank to ship the repo with no urgency UI by default.
#
# NEXT_PUBLIC_FOUNDERS_MANUAL_SOLD — count of founders sales closed
# OUTSIDE Stripe (e.g. Skool community, invoices, manual deals) that the
# Stripe webhook can't see. Added on top of the real Stripe count and
# capped at slotsTotal. Bump this value each time you close an off-Stripe
# sale; Stripe sales accumulate automatically.
# ======================
NEXT_PUBLIC_FOUNDERS_MANUAL_SOLD=

# ======================
# Post-payment GitHub auto-invite (AgentStack-variant only — optional)
# Powers the "Get instant repo access" button on /thank-you. When a
# buyer enters their GitHub username, the server adds them to the
# configured team via the GitHub API — exactly the same path used for
# manual additions today, just automated.
#
# Without these vars: /api/github/invite returns a friendly error and
# buyers fall back to the manual chat path.
#
# Setup:
#   1. Go to https://github.com/settings/tokens?type=beta (fine-grained PAT)
#   2. Resource owner: the org (e.g. Claude-Code-Pro-Camp)
#   3. Repository access: All repositories (or specific repo, doesn't matter for team writes)
#   4. Organization permissions:
#        - Members: Read and write
#   5. Generate token, copy into GITHUB_TOKEN below
#   6. Set GITHUB_INVITE_ORG to the org slug
#   7. Set GITHUB_INVITE_TEAM to the team slug buyers should be added to
#
# Security model: the buyer's claim token (256-bit random, in their
# /thank-you URL only) is hashed with SHA-256 on the server. Only the
# hash is persisted on purchases/{sessionId}. The raw token never
# touches our database.
# ======================
GITHUB_TOKEN=
GITHUB_INVITE_ORG=
GITHUB_INVITE_TEAM=

# ======================
# GUIDED DNS CUTOVER
# ======================
# NEXT_PUBLIC_TARGET_NAMESERVERS — the nameserver pair the DNS wizard tells
# agents to switch their domain to. Comma-separated, two or more entries.
#
# LEAVE THIS EMPTY unless this deployment actually operates DNS for customer
# domains. Empty is the safe, supported configuration: the wizard guides the
# agent to add an A record or CNAME at the DNS host they already use, the
# domain never changes hands, and their email cannot break.
#
# A nameserver pair is issued per account by the DNS provider and only answers
# for zones inside that account. Pointing a customer's domain at a pair that
# has no zone for it does not degrade their site — it removes the domain from
# DNS entirely. Website down, mail down, until they revert and wait out
# propagation. A wrong value here is worse than no value, so the code treats
# anything malformed or single-entry as unset.
NEXT_PUBLIC_TARGET_NAMESERVERS=
